# Profil "dev" — surcharges locales pour ne pas saturer le disque/réseau.
# Activer via : -Dspring.profiles.active=dev (ou SPRING_PROFILES_ACTIVE=dev)
# La limite de 1 Go est désormais définie directement dans application.properties.
